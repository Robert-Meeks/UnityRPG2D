using UnityEngine;
using System.Collections;

public class SortParticleSystem : MonoBehaviour {

    //not required because unity added the ability to assign sorting layer in the same way you would for everything else used thus far
}
